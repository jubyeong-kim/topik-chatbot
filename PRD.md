# PRD: TOPIK 채점 안내봇 — v2

> **v2 개정 (2026-08-28)** — v1 을 검사한 결과 **23건**을 고쳤다.
> 그중 셋(`F1` CORS · `F18` PBT/IBT 충돌 · `F20` 거절 오작동)은 **그대로 구현하면 멈추는** 문제였다.
> 검사 전문: `~/.claude/plans/prd-sequential-newt.md` · v1 원본: `PRD-topik-bot.v1.bak`

**표기 규칙** (저장소 공통) — `[미확보]` `[출처 약함]` `[미검증]` `🔴 임의`

---

## 0. 로컬 환경

| 항목 | 상태 | 비고 |
|---|---|---|
| OS | Windows 10 | ⚠️ v1 의 `brew` 설치 안내는 **오류였다** (§9 정정) |
| RAM | 8GB | 브라우저 임베딩 + LLM 동시 구동 |
| Ollama | 미설치 | §9 |
| LLM | `qwen2.5:1.5b-instruct-q4_K_M` | **986MB** (v1 의 "1.2GB" 정정), 32K 컨텍스트 |
| 임베딩 | `multilingual-e5-small` (384차원), dtype **`q8`** | v1 의 `no_gather_q4` 는 **존재하지 않는 값**이었다 |
| 빌드 | 네이티브 컴파일 패키지 금지 | 이 PC 에 VS 빌드도구 없음 |

---

## 1. 배경 및 목적

**제품명**: TOPIK 채점 안내봇

**무엇을 만드는가**
한국어 교사와 학습자가 **TOPIK 쓰기가 어떻게 채점되는지** 자연어로 물으면,
공개 자료에 근거해 답하고 **출처를 함께 제시하는** 브라우저 기반 RAG 챗봇.

**대상**: 한국어 교사 + TOPIK 학습자

**질문 범위**: **TOPIK 쓰기 채점** — 채점 기준·배점·감점 조건·문항 구조·등급 기준

> v1 은 "한국어 교육 전반"이었다. **좁혔다.** 확보 자료의 폭과 범위를 일치시키기 위해서다.
> 범위를 넓게 두고 "무조건 거절"을 택하면 데모의 대부분이 거절이 된다.

**자료 밖 질문**: **무조건 "모른다"고 거절한다.** 일반 지식으로 보완하지 않는다.

**대화 형태**: **단일 턴.** 후속 질문(*"그럼 5급은요?"*)은 1차 범위 밖이다.

> 질문을 그대로 임베딩하는 구조에서 후속 질문은 검색이 깨진다. 재작성 로직은 2차로 미룬다.

**학습 목표 대응**

| # | 학습 목표 | 위치 |
|---|---|---|
| 1 | PRD 부터 스스로 질문하며 기획 | §2 |
| 2 | 자료 수집 → 청크/임베딩 → 브라우저 벡터스토어 | §6~§8 |
| 3 | 로컬 Ollama 스트리밍 + 브라우저 임베딩 RAG | §8~§9 |
| 4 | 근거성/거절 판정 + 사람 피드백 | §11 |
| 5 | 루브릭 기반 실험 및 표 기록 | §12 |
| 6 | 정적 배포 후 수용 기준 재확인 | §13 |

---

## 2. 기획 자문자답

1. **누가 쓰는가?** → 교사 + 학습자
2. **어떤 질문에 답하는가?** → TOPIK 쓰기 채점
3. **무엇을 모르면 "모른다"고 하는가?** → 수집 자료 범위 밖 전부
4. **근거가 약할 때는?** → §10 약한 근거 배지
5. **공식과 해설을 어떻게 구분하는가?** → §7 `official` 필드 · §10 회색 칩
6. **성공/실패 판정은?** → §11 3축 판정

---

## 3. 범위

**포함** — TOPIK 쓰기 채점 질의응답 · 출처 표기 · 근거 모달 · 검색 단계 시각화 · 스트리밍 + 취소

**제외** — 실제 학습자 작문 채점 · 서버 DB · 로그인 · 멀티턴 · 한국어 교육 전반 질문

---

## 4. 앱 구성

- **스택**: Vite + React + TypeScript
- **실행**: **로컬 우선** (`npm run dev`). GitHub Pages 는 **데모용** (학습 목표 6)
- `vite.config.ts` 의 `base: '/topik-chatbot/'`

```text
/src
  /components   채팅창, 출처칩, 근거모달, 진행률 바, 배지
  /data         chunks.json
  /lib          embed / search / prompt / ollama
/scripts        build-vectorstore.mjs
/eval           questions.json (봉인)
/public
  /vectorstore  vectorstore.json (사전 계산 결과)
```

---

## 5. 화면 구성

**단일 페이지다.** 라우팅을 두지 않는다 — 대화가 단일 턴이고 화면 간 이동이 없다.
대신 **한 질문이 지나는 단계를 화면에 드러낸다.**

| 영역 | 언제 보이나 | 무엇을 보여주나 |
|---|---|---|
| **머리말** | 항상 | 제목 · 한 줄 설명 |
| **질문 입력** | 항상 | 입력창 · `물어보기` · **`중단`**(생성 중에만) |
| **연결 상태 배너** | Ollama 미실행·모델 없음 | 사유 + **`다시 확인`** 버튼 |
| **예시 질문 칩** | 항상 | 4개. **1층 거절 사례(응시료)를 일부러 포함**한다 |
| **준비 상태 줄** | 모델 로딩 후 | 모델명 · dtype · 차원 · 청크 수 · **캐시 사용 여부** · 준비 시간 |
| **진행 표시** | 처리 중 | 버튼 라벨이 `검색 → 판단 → 작성 → 판정` 으로 바뀐다 |
| **거절 카드** | 1층·2층에서 막혔을 때 | *"답할 수 있는 자료를 찾지 못했습니다"* + **사유** |
| **답변** | 생성 중부터 | 스트리밍. 커서(▌)로 생성 중임을 표시 |
| **판정 배지** | 답변 완료 후 | 점수 · 근거성 · 환각없음 · 출처표기 · **한계 고지** |
| **피드백** | 답변 완료 후 | 👍/👎 · 누적 건수 · **자동 판정과 어긋난 건수** |
| **근거 목록** | 답변과 함께 | 청크마다 공식/비공식 칩 · 검색 경로 · 점수 · 본문 140자 |
| **근거 모달** | 근거를 눌렀을 때 | **원문 전체 + 출처 링크** |
| **한계 고지** | 항상 (하단) | 개인 첨삭 아님 · 자료 밖 거절 · PBT/IBT 구분 |

> **왜 중간 과정을 드러내나**: 답이 자연스럽다는 사실과 근거를 가졌다는 사실은 다르다.
> 어느 단계에서 멈췄는지, 어떤 검색기가 그 근거를 데려왔는지 보이지 않으면
> 사용자는 그 둘을 구분할 수 없다.

### 소개 화면

- 제목: "TOPIK 채점 안내봇 — 쓰기 채점, 무엇이든 물어보세요"
- 설명: "TOPIK 쓰기 채점 기준을 공개 자료에 근거해 안내합니다."
- **예시 질문 — 확보한 자료로 실제 답이 되는 것만 쓴다** (v1 은 이 검증이 없었다)
  - "54번은 어떤 기준으로 채점되나요?" → 자료 A 채점 기준표
  - "53번에서 감점되는 경우는 뭔가요?" → 자료 A 감점 조건
  - "3급과 5급의 평가 기준은 뭐가 다른가요?" → 자료 D
- 한계 고지
  - "학습자 개인 작문을 채점해주는 기능은 아닙니다"
  - "수집된 자료에 없는 내용은 답하지 않습니다"
  - "PBT 기준과 IBT 기준이 다릅니다 — 답변에 어느 쪽인지 표시됩니다"

---

## 6. 원천 자료 — 실사 완료 (2026-08-28)

| 자료 | 접두어 | 내용 | 청크 | 상태 |
|---|---|---|---|---|
| **A. TOPIK 공식 「TOPIK Ⅱ 쓰기 답안 작성 방법」** | `TPK-` | 51·52·53·54 전 문항. **채점 기준표 2개**, 감점·주의 조건 24개 | 40~46 | 🟢 |
| **B. 자체 문법 규칙** | `GRM-` | 조사 생략·띄어쓰기·직역체 등 7종 | 7~12 | 🟢 |
| **C. 시험 구조** | `EXM-` | IBT 3문항/50분/200점, PBT 대비 | 10~15 | 🟢 |
| **D. 등급별 평가기준** | `LVL-` | 1~6급 기술 | 6~12 | 🟢 |
| **F. 공식 숫자** | `NUM-` | 등급 컷·영역 배점·문항 수 | 5~8 | 🟡 `[출처 약함]` |
| **G. 자체 해설** | `EXP-` | 공식 자료끼리 잇는 해석 | 6~10 | ⚪ **비공식** |

**총 74~103 청크.**

> ⚠️ **주관 기관**: TOPIK 은 **국립국제교육원(NIIED)** 이 시행한다.
> v1 이 "국립국어원"으로 적은 것은 오류였다. 국립국어원은 「한국어 표준 교육과정」 쪽이다.

**제외**: 「한국어 표준 교육과정」(범위 축소) · `data/papers/` 학술 논문

> 논문 제외는 저작권 때문이다. `vectorstore.json` 은 **공개 배포물**이라 원문 청크 삽입은 재배포다.

### 청크 원칙

- 문단이 아니라 **"하나의 사실/주장"** 단위
- 출처(URL 또는 문서명 + 섹션명) **필수**
- 길이 **200자 내외 권장** — 단, **표는 통청크 허용**
  > 채점 기준표는 실측 **417자 / 442자**다. 쪼개면 "언어 사용"이 몇 점짜리인지 사라진다
- 자료 B 는 **type 별 7종으로 묶어 재작성**한다
  > 원본 43행은 `explanation` 이 템플릿 반복이라 그대로 넣으면 서로 경쟁해 검색이 뭘 물어도 같은 답을 낸다
- **자료 D 는 등급당 1청크** (쪼개면 등급 비교 질문에서 한쪽만 검색된다)

### 자료 A 추출 방법 (재현용)

```bash
pdftotext -enc UTF-8 -layout 원본.pdf -
```

⚠️ **`-enc UTF-8` 을 빼면 한글이 통째로 사라진다.** 채점 기준표 2개는 열이 섞여 나오므로 사람이 재구성한다.

---

## 7. 청크 스키마

```json
{
  "id": "TPK-001",
  "text": "54번은 내용 및 과제 수행 12점, 글의 전개 구조 12점, 언어 사용 13×2=26점으로 총 50점이다.",
  "source_title": "TOPIK Ⅱ 쓰기 답안 작성 방법",
  "source_url": "https://exam.topik.go.kr/...",
  "category": "채점기준",
  "official": true,
  "exam_mode": "PBT",
  "created_at": "2026-08-28"
}
```

| 필드 | 설명 |
|---|---|
| `id` | 자료별 접두어 + 순번. **재사용 금지** |
| `text` | 사실 단위 원문 (임베딩 대상) |
| `source_title` | 문서명. **URL 이 깨져도 추적 가능해야 하므로 필수** |
| `source_url` | 출처 링크. 자체 해설은 `null` |
| `category` | 검색 필터 · UI 배지 |
| **`official`** | `true` = 공식 자료, `false` = 자체 해설 |
| **`exam_mode`** | `"PBT"` \| `"IBT"` \| `"공통"` |

> `source_url` 주의: 자료 A 의 URL 은 CMS 업로드 경로(`.../uploadId/2024/02/...`)라 **재업로드되면 깨진다.**

### ⚠️ `exam_mode` 가 필요한 이유

| | PBT | IBT |
|---|---|---|
| 쓰기 문항 | **4문항** (51·52·53·54) | **3문항** |
| 쓰기 배점 | 10/10/30/50 = **100점** | **200점** |
| 시험 총점 | 300점 | **600점** |

자료 A 는 PBT, 자료 C 는 IBT 다. 구분하지 않으면 봇이
**"쓰기는 100점"과 "쓰기는 200점"을 동시에 확신 있게 주장한다.**

---

## 8. 임베딩 및 벡터스토어

- **모델**: `multilingual-e5-small` (384차원), **dtype `q8`**
  > v1 의 `no_gather_q4` 는 transformers.js 에 없는 값이다 (`fp32/fp16/q8/q4/q4f16/bnb4/int8/uint8`).
  > 384차원 소형 모델은 q4 의 용량 이득이 작고 정확도 손실만 크다.
- **⚠️ e5 는 접두어가 필수다** — 안 붙이면 검색 품질이 눈에 띄게 떨어진다
  - 청크 임베딩 → `passage: {text}`
  - 질문 임베딩 → `query: {질문}`
- **정적 벡터스토어**: `scripts/build-vectorstore.mjs` 로 사전 계산 → `public/vectorstore.json`.
  런타임엔 **질문 임베딩만** 계산
- **캐시·진행률**: 진행률 %와 **캐시 사용 여부**를 함께 표시한다
  > Hugging Face 의 resolve URL 은 서명된 CDN URL 로 리다이렉트되어 HTTP 캐시 적중이 불안정하다.
  > 재방문에서 모델 준비가 아니라 질문에 집중하게 만드는 것이 캐시의 목적이므로,
  > 캐시에서 꺼냈는지 새로 받았는지를 **사용자에게 알린다.**
- **⚠️ 공간 일치 검사 (필수)**: 질문 벡터의 차원이 `vectorstore.json` 의 `dim` 과 다르면 **오류로 멈춘다**
  > 차원이 우연히 같아도 다른 모델의 공간을 섞으면 숫자는 나오지만 "가까움"의 뜻이 보장되지 않는다.
  > 그래서 벡터스토어에 `model`·`dtype`·`dim` 을 기록하고, 런타임은 **그 값을 그대로 읽어** 모델을 연다.
  > 기존 스크립트가 있다는 이유로 재실행하지 않는다 — 코퍼스의 생성 모델부터 확인한다.
- 검색 품질이 부족하면 768차원으로 전환한다 (청크 텍스트 재사용, 벡터스토어만 재생성)

### 브라우저 실행 확인 (2026-08-28 실측)

| 항목 | 결과 |
|---|---|
| WASM 에서 q8 로드 | ✅ 성공 |
| 차원 일치 | ✅ 384 = 384 |
| 콜드 준비 시간 | **39.2초** (모델 최초 다운로드 포함) |

> 📌 **`model_no_gather_q4.onnx` 우회는 우리에게 필요 없다.**
> 그 우회는 `embeddinggemma-300m-ONNX` 에서 WASM ONNX Runtime 이
> `GatherBlockQuantized` 를 지원하지 않아 생기는 문제다. 4비트 블록 양자화 임베딩 테이블에서 나오는데,
> **e5-small 을 q8 로 쓰면 해당 연산이 없다.** 실제로 브라우저에서 그냥 로드된다.
> (v2 가 이 파일명을 "존재하지 않는 값"이라고 적은 것은 부정확했다 —
> `dtype` 파라미터 값으로는 없지만 **파일명으로는 실재한다.**)

---

## 9. LLM 연결 (Windows)

**모델**: `qwen2.5:1.5b-instruct-q4_K_M` (986MB)

### 설치

1. [ollama.com/download](https://ollama.com/download) 에서 **Windows 설치 파일**을 받아 설치
2. 모델을 받는다

```bash
ollama pull qwen2.5:1.5b-instruct-q4_K_M
```

3. 동작을 확인한다

```bash
curl http://localhost:11434/api/tags
```

### ⚠️ GitHub Pages 에서 쓰려면 환경변수가 필요하다

Ollama 는 **localhost 에서 서빙된 페이지만** CORS 를 허용한다.
`https://<아이디>.github.io` 출처는 preflight 에서 **거부된다.**

```bash
setx OLLAMA_ORIGINS "https://<아이디>.github.io"
```

설정 후 **Ollama 를 재시작**해야 적용된다.

- **지원 브라우저**: **Chrome · Edge**. Safari 는 https 페이지에서 `http://localhost` 를 mixed content 로 차단한다
- **로컬 실행(`npm run dev`)에서는 이 설정이 필요 없다** — 그래서 개발·평가는 로컬에서 한다

### 연동 요구사항

- **헬스체크**: 최초 로딩 시 `http://localhost:11434` 확인 → 실패 시 안내 메시지
- **스트리밍**: `/api/chat` 을 `stream: true` 로 호출, 토큰 단위 렌더
- **취소**: `AbortController` 로 중단

---

## 10. 검색 · 프롬프트 · UI

### 순위 — 두 검색기를 섞지 않고 합친다 (v3, 실측으로 변경)

**코사인 상위 10개**를 먼저 뽑고, 그 안에 없는 것 중 **BM25 상위 5개**를 덧붙인다.
각 후보에 **`method`(`vector` / `bm25`)와 점수를 보존**해 화면에 그대로 보여준다.

| 검색기 | 잡는 것 | 놓치는 것 |
|---|---|---|
| `vector` (코사인) | 표현이 달라도 가까운 의미 | 고유명사·문항번호·정확한 표기 |
| `bm25` | 질문 낱말이 그대로 있는 문서 | 다른 말로 쓴 같은 뜻 |

> **왜 가중합을 버렸나.** `0.7*cos + 0.3*bm25` 로 섞었더니 **짧은 청크가 낱말 하나로 점수를 벌어
> 긴 근거 청크를 밀어냈다.** *"54번은 어떤 기준으로 채점되나요?"* 에서 정답인 채점 기준표가 **17위**였고,
> *"54번은 개조식으로 쓰지 마라"* 같은 한 줄짜리가 위로 왔다.
>
> 두 검색기는 **경쟁시킬 대상이 아니라 실패 방식이 서로 다른 두 관찰 장치**다.
> 합집합은 그 오염이 구조적으로 생기지 않는다.

**변경 전후 실측** (`scripts/diag-rank.mjs`, 기대 청크 15문항)

| | 가중합 | **합집합** |
|---|---|---|
| 정답 청크가 후보에 포함 | — | **14/15** |
| 코사인 top-1 적중 | 6/15 | **10/15** |
| 최악 사례(54번 채점기준) | **17위** | **6위, 후보 포함** |
| BM25 가 단독으로 살린 것 | — | 1건 (*"53번과 54번의 배점 차이"* → `NUM-005`) |

> 유일한 미포함은 *"글자 수를 못 채우면 어떻게 되나요?"* 인데, **검색 실패가 아니다.**
> 자료 A 는 분량(200~300자·600~700자)만 정하고 **미달 시 처리를 적지 않았다.**
> 즉 코퍼스에 답이 없는 질문이고, 기대 청크를 적은 쪽이 틀렸다.
> → 2층 LLM 게이트가 이런 것을 거절해야 한다.

**중복 제거는 필수다.** 같은 사실이 여러 번 들어가면 모델이 그 청크를 과대평가한다.

### 🔴 거절 판정 — 실측으로 설계가 뒤집혔다 (v3, 2026-08-28)

**코사인 임계값 거절은 폐기했다.** 봉인 질문 20개로 측정하니 자료 안과 밖이 겹친다.

| 지표 | 자료 안 15개 | 자료 밖 5개 | 판정 |
|---|---|---|---|
| top1 코사인 | 0.838 ~ 0.900 | 0.850 ~ 0.875 | **겹침** |
| top1 − 평균 | 0.041 ~ 0.107 | 0.038 ~ 0.069 | **겹침** |
| z-점수 | 1.40 ~ 3.79 | 1.94 ~ 2.72 | **겹침** |
| 상위권 낙폭 | 0.005 ~ 0.061 | 0.009 ~ 0.030 | **겹침** |

> 자료 밖 질문도 전부 TOPIK 이야기다. 임베딩이 재는 것은 **주제 근접성**이지
> *"이 청크가 이 질문의 답이 되는가"* 가 아니다. **어떤 임계값으로도 못 자른다.**
> 재현: `scripts/diag-threshold.mjs`

### 거절은 3층이다

| 층 | 무엇 | 성격 | 상태 |
|---|---|---|---|
| **1층 범위 규칙** | §1·§3 범위 정의에서 끌어낸 어휘 필터 (쓰기 외 영역 · 시험 운영 · 진로 · 교수법 · 첨삭 요청) | 결정적 · 설명 가능 | ✅ 봉인 20/20 |
| **2층 LLM 게이트** | *"제공된 자료로 이 질문에 답할 수 있는가"* 를 모델에 묻고 아니면 거절 | 확률적 | ⏳ Ollama 설치 후 |
| **3층 약한 근거** | 어휘 일치가 없으면 배지를 단다. **거절이 아니라 표시** | 보조 | ✅ |

> ⚠️ **1층만으로는 부족하다.** 범위 필터는 *어휘로 드러나는* 범위 밖만 잡는다.
> *"54번에서 맞춤법 하나 틀리면 몇 점 깎이나요?"* 처럼 **범위 안처럼 보이지만 자료에 답이 없는**
> 질문은 그대로 통과한다 (진단 4개 중 4개 통과 — `scripts/probe-unanswerable.mjs`).
> **그것이 2층이 필요한 이유다.** 2층 없이는 §1 의 "무조건 거절" 원칙이 절반만 지켜진다.

> 📌 **1층 목록을 봉인 질문에 맞춰 고치지 않는다.** 범위 정의에서만 끌어낸다.
> 어긋나는 사례가 나오면 목록이 아니라 기록을 고친다.

BM25 합성 점수는 **순위에만** 쓴다. 거절 판정에는 쓰지 않는다 —
정규화가 질의 내 상대값이라 자료가 없어도 최상위가 1.0 이 되기 때문이다.

### 프롬프트 규칙

- 시간 컨텍스트 포함 ("오늘 날짜: …")
- 자료 밖이면 "관련 자료를 찾지 못했습니다"
- **시험 방식을 밝히지 않은 질문에는 어느 쪽 기준인지 함께 답한다**
  > *"54번은 몇 점인가요?"* → *"PBT 기준 50점입니다. IBT 는 3문항 구성이라 문항 번호가 다릅니다."*
- 답변 끝에 사용한 청크 ID 표기: `[TPK-003, LVL-005]`

### UI

| 요소 | 역할 |
|---|---|
| 출처 칩 | `official: true` → **파란 칩** + 링크 · `official: false` → **회색 칩 + "비공식" 배지** |
| 근거 모달 | 칩 클릭 시 원문 청크 전체 + `source_url` |
| 검색 단계 | "질문 임베딩 중 → 청크 검색 중 → 답변 생성 중" |
| 약한 근거 배지 | 위 2단 거절의 2번으로 통과했을 때 |
| 비공식 고지 | 비공식 청크만으로 답했을 때 "공식 자료에 없는 설명입니다" |
| 시험 방식 표시 | 답변에 쓰인 청크의 `exam_mode` |

---

## 11. 판정 및 피드백

**3축 판정** (v1 은 2축이었다)

1. **근거성** — 답변이 제시된 청크에 실제로 근거했는가
2. **거절 적절성** — 관련 청크가 없을 때 실제로 "모른다"고 했는가
3. **공식성** — **공식 근거로 답했는가, 해설로 답했는가**

> 판정 모델: **사람이 기본.** 답변 생성과 같은 1.5B 가 자기 답을 채점하면 판정을 신뢰할 수 없다.
> 보조로 더 큰 모델을 쓸 수 있으나 사람 판정을 대체하지 않는다.

**사람 피드백**: 답변 하단 👍/👎 → 로컬 저장. 판정과 불일치한 사례를 따로 검토한다.

---

## 12. 실험 및 루브릭

**⚠️ 봉인 질문 세트가 전제다.** 고정 세트 없이는 회차 간 비교가 성립하지 않는다.

`eval/questions.json` — 자료 안 15개 + 자료 밖 5개.
**자료 목록(제목·범주)만 보고 쓰고, 청크 본문은 보지 않는다.**

> 본문을 보고 쓰면 청크에 맞춘 질문이 되어 측정이 무의미해진다.
> 이 프로젝트엔 측정 편향으로 결론이 뒤집힌 이력이 있다 (`topik-ocr-poc/RETROSPECTIVE.md`).

**실험 변수** — 상위 K(3 vs 5) · **코사인/BM25 비율** · 긴 청크 처리 · LLM 모델 크기 · WASM vs WebGPU

### ⚠️ 지표를 하나 더 쓴다 — 검색 적중률

거절/답변 여부만 재면 **엉뚱한 청크로 답해도 통과**한다. 실제로 그랬다.

| 순위 방식 | top-1 적중 | top-3 적중 |
|---|---|---|
| 합성 `0.7*cos + 0.3*bm25` | **6/15** | 11/15 |
| **코사인 단독** | **10/15** | 10/15 |

> **BM25 가중치가 top-1 을 악화시킨다.** 최악은 *"54번은 어떤 기준으로 채점되나요?"* 로,
> 정답인 채점 기준표(TPK-024)가 **17위**다. 대신 *"54번은 개조식으로 쓰지 않아야 한다"* 같은
> **짧은 청크**가 '54번' 이라는 어휘로 올라온다.
>
> 원인은 §6 의 통청크 예외와 직결된다. 442자짜리 표는 mean pooling 에서 주제가 희석되고
> BM25 길이 정규화(b=0.75)에서도 불리하다. **표를 통으로 두는 대가다.**
> 재현: `scripts/diag-rank.mjs`

**후보 조치 (아직 정하지 않음)** — 셋 다 §12 실험으로 판정한다.
1. BM25 가중치를 낮추거나 없앤다
2. 긴 청크에 제목을 붙여 `passage: {제목}. {본문}` 으로 임베딩한다
3. BM25 길이 정규화 `b` 를 낮춰 긴 청크 페널티를 줄인다

> 🔴 **주의**: 위 15개 기대 청크는 **코퍼스를 만든 뒤에 붙인 주석**이다.
> 이 표를 보고 가중치를 맞추면 그 순간 측정이 아니라 조정이 된다.
> 가중치를 바꾸면 **봉인 세트로 거절 성능도 함께 다시 잰다.**

**루브릭 — 무엇을 좋아졌다고 볼 것인가**

인상이 아니라 셀 수 있는 것으로 적는다. 목표값은 남의 수치를 베끼지 않고 실행 결과를 보고 정한다.

| 지표 | 측정 방법 | 목표 | 현재 (2026-08-28) |
|---|---|---|---|
| **거절 정확도** | 봉인 20 + 진단 5 중 맞힌 수 | 25/25 | **21/25** |
| **과소거절** | 자료 밖인데 답한 수. §1 원칙의 생사 | **0** | **0** ✅ |
| **검색 recall** | 정답 청크가 후보에 들어온 비율 | 15/15 | **14/15** |
| **출처 표기** | 답변에 `[ID]` 가 있는 비율 (코드로 셈) | — | **0/4** ❌ |
| **첫 토큰까지** | 게이트 + 생성 시작 | **20초** (§13 개정) | **~25초** (콜드) |

> 유일한 미포함 recall 1건과 거절 오판 2건은 **자료에 답이 없어서 맞는 판정**이다.
> 실제 오판은 1건(*"띄어쓰기 실수도 채점에 반영되나요?"*).

---

### 실행 기록 — 한 번에 하나만 바꾼다

> ⚠️ **오늘 이 규칙을 두 번 어겼고 두 번 다 대가를 치렀다.**
> 형식과 문구를 동시에 바꿔 원인을 못 가렸고, 3문항 평균을 내 개별 편차를 가렸다.
> 세 번째로는 **같은 프롬프트를 반복해 Ollama KV 캐시가 섞였다**(0.2초 = 캐시 적중).
> 측정할 때는 **설정마다 다른 질문**을 쓰고 **개별 실행을 그대로** 남긴다.

#### A. 검색 순위 방식 (`scripts/diag-rank.mjs`)

| 회차 | 변경 | recall | top-1 | top-3 | 판단 |
|---|---|---|---|---|---|
| A1 | 가중합 `0.7*cos + 0.3*bm25` | — | 6/15 | 11/15 | 짧은 청크가 낱말 하나로 긴 근거를 밀어냄 |
| **A2** | **합집합 (코사인 10 + BM25 5)** | **14/15** | **10/15** | 10/15 | ✅ 채택. BM25 가 1건 단독 구제 |

#### B. 거절 판정 (`scripts/eval-retrieval.mjs` · `eval-gate.mjs`)

| 회차 | 변경 | 자료 안 | 전체 | 판단 |
|---|---|---|---|---|
| B1 | 코사인 임계값 단독 | — | 15/20 | ❌ 자료 안/밖 코사인이 겹쳐 **원리적으로 불가** |
| B2 | 1층 범위 규칙 추가 | — | 20/20 | ✅ 단, 어휘로 드러나는 것만 잡음 |
| B3 | 2층 게이트 (엄격 기준, 청크 5) | 10/15 | 20/25 | |
| B4 | ↳ JSON 스키마 강제 | 5/15 | 15/25 | ❌ **1.5B 는 형식 맞추느라 판단을 잃는다** |
| B5 | ↳ 프롬프트 어휘를 `answerable` 로 | 3/15 | 13/25 | ❌ **모델이 프롬프트 낱말을 그대로 뱉어 파서가 깨짐** |
| B6 | 기준 완화 + YES/NO 어휘 복귀 | 11/15 | 21/25 | ✅ |
| **B7** | **게이트 청크 5 → 4** | **12/15** | **22/25** | ✅ **속도 절반인데 정확도 +1** |

#### C. 게이트 지연 분해 (`scripts/bench-gate2.mjs`)

| 설정 | 게이트(중앙) | 프롬프트 | 생성 |
|---|---|---|---|
| 청크 8 · `num_predict` 5 | 29.2초 | ~1,000토큰 / 34 tok/s | 2토큰 / **0.2초** |
| 청크 8 · `num_predict` 1 | 44.2초 | ~1,350토큰 / 31 tok/s | 1토큰 / **0.0초** |
| **청크 4** · `num_predict` 5 | **13.8초** | ~560토큰 / 39 tok/s | 2토큰 / 0.2초 |

> **`num_predict` 는 레버가 아니다.** 생성 토큰이 1~2개뿐이라 아낄 시간이 0.2초다.
> 프롬프트 처리 속도는 설정과 무관하게 **30~40 tok/s 로 일정**하다.
> 즉 **게이트 시간 ≈ 읽을 토큰 수 ÷ 35.** 레버는 **청크 수**다.
>
> 그래서 게이트와 생성에 다른 개수를 쓴다 — `GATE_K=4`, `ANSWER_K=8`.
> (한때 앱은 8개, 평가 스크립트는 5개를 넣어 **측정과 동작이 달랐다.** 지금은 상수를 공유한다.)

#### D. 모델 크기 (`scripts/bench-model.mjs`) — 3b 는 기각

| | `1.5b-q4_K_M` | `3b-q4_K_M` |
|---|---|---|
| 게이트 왕복 | 28.6초 | **68.4초** |
| 첫 토큰까지 | 29.6초 | **60.8초** |
| 생성 속도 | 3.0 tok/s | **1.2 tok/s** |
| **질문 하나당** | **101초** | **165초** |
| 프로세스 메모리 | 1,397MB | 2,240MB |
| 페이지 파일 최고 | — | **2,676MB** |

> **3b 는 죽지 않는다. 다만 스왑한다.** 3문항 모두 성공했지만 디스크 페이징이 2.6GB 까지 올라
> 토큰마다 디스크를 읽는다. `[미검증]` 은 이것으로 풀렸다 —
> **"안 돌아간다"가 아니라 "돌지만 실용 속도가 아니다".**
>
> ⚠️ 1.5b 수치는 3b 다운로드가 대역폭·CPU 를 쓰는 중에 측정됐다. 불리한 조건이므로 재측정이 필요하다.

#### E. 판정기 (`scripts/eval-judge.mjs`)

| 회차 | 변경 | 결과 |
|---|---|---|
| E1 | 6필드 전부 모델에게 물음 | ❌ `cited=true` 인데 답변에 `[ID]` **하나도 없음** (2/2 오판) · `noHalluc=false` 인데 `score=85` 모순 · `comment` 가 평어가 아니라 **질문에 다시 답함** |
| **E2** | **`cited`·`score` 를 코드로 계산 + "답을 다시 쓰지 마십시오"** | ✅ `cited` 정확 · `comment` 가 평어가 됨 |

> **셀 수 있는 것은 모델에게 묻지 않는다.** 이것이 E1→E2 의 교훈이다.
>
> 남은 한계: `noHalluc` 이 **측정 4건 전부 false** 다. 잘 쓴 답변에도 false 라
> 점수를 늘 40점 깎는다. 필드명이 이중부정("지어내지 **않았는가**")이라
> 1.5B 가 방향을 놓친다는 가설이 있으나 **아직 검증 안 함** `[가정]`.

---

#### F. BM25 길이 정규화 `b` — 효과 없음, 기각

| `b` | recall | top-1 | top-3 |
|---|---|---|---|
| 0.75 (기준) · 0.30 · 0.00 | 14/15 | 10/15 | 10/15 |

> 세 값이 **완전히 같다.** 합집합 구조에서 BM25 는 순위가 아니라 *"추가 후보 5개"* 만 담당하므로,
> 길이 정규화를 어떻게 놓아도 뽑히는 청크가 바뀌지 않는다. **가중합이었다면 달랐을 실험이다.**

#### G. 긴 청크에 검색용 제목 (`index_text`) — 채택, 단 맞교환

200자 넘는 청크 5개에 **검색용 짧은 표현**을 붙이고 그것으로만 임베딩한다.
표시는 원문 그대로다 (index summary / return full).

```json
{ "id": "TPK-024",
  "text": "54번 채점 기준(총 50점). [내용 및 과제 수행 12점] 1) …",   // 442자, 화면 표시용
  "index_text": "54번 채점 기준표. 내용 및 과제 수행 12점, …" }  // 임베딩용
```

| | 전 | 후 |
|---|---|---|
| recall | 14/15 | 14/15 |
| top-1 | 10/15 | 10/15 |
| **top-3** | 10/15 | **12/15** |
| **TPK-024 순위** (*"54번은 어떤 기준으로 채점되나요?"*) | **6위** | **1위** ✅ |
| 1층 거절 (봉인 20) | 20/20 | 20/20 |
| **2층 게이트 (25문항)** | **21/25** | **21/25** — 동점 |

> ⚠️ **한때 "22 → 20 맞교환"이라고 적었다. 그 서술은 틀렸다.**
> 서로 **다른 시점에 따로 잰 값**을 비교한 것이고, 그사이 게이트 프롬프트와 `GATE_K` 가
> 함께 바뀐 상태였다. 비교 조건이 같지 않았다 — 측정 방식에서 "한 번에 하나만" 을 어겼다.
>
> **같은 실행에서 전/후를 나란히 재니 21/25 로 동점이다** (`scripts/diag-tradeoff2.mjs`).

**뒤집힌 문항은 2개이고 서로 상쇄된다.**

| 질문 | 전 | 후 | 기대 | |
|---|---|---|---|---|
| *"53번과 54번의 배점 차이는?"* | YES | **NO** | YES | ❌ `TPK-025`(등급별 배점표)가 상위 4개에서 빠짐 |
| *"51번과 52번은 어떻게 다른가요?"* | NO | **YES** | YES | ✅ 무관한 채점 기준표가 빠지고 문항 설명이 들어옴 |

> **`index_text` 의 실제 효과**: 긴 청크가 **덜 자주, 더 정확하게** 뽑힌다.
> 짧은 검색용 문장으로 임베딩하니 주제가 또렷해져 **정말 관련된 질문에서만** 상위로 오른다.
> 그것이 한 문항에는 손해, 한 문항에는 이득이었다.
>
> 게이트를 타는 20문항 중 **12문항의 상위 4개 구성이 바뀌었다** (`scripts/diag-tradeoff.mjs`).
> 그런데도 판정은 2개만 뒤집혔다 — 구성이 바뀌어도 대개 같은 결론에 도달한다.

**게이트는 결정적이다.** 같은 입력을 두 번 물었을 때 20문항 전부 같은 답이 나왔다(흔들림 0).
`temperature: 0` 이 실제로 지켜진다는 뜻이고, 앞으로 회차 간 비교를 신뢰할 수 있다는 뜻이다.

#### H. 답변에 `[ID]` 표기 강제 — 되돌림

지시를 시스템 규칙 목록(5번)에서 **프롬프트 맨 끝**으로 옮겼다.

| | 전 | 후 |
|---|---|---|
| 인용률 | 0/2 | **2/2** |
| 답변 본문 | 정상 | ❌ **한 건이 80자로 붕괴** — 본문이 사라지고 `[TPK-024]` 만 남음 |

> **끝에 둔 지시가 답변을 삼켰다.** 템플릿 블록 때와 같은 실패다.
> 본문이 인용 표기보다 중요하므로 되돌렸다.
> **추적 가능성은 화면의 '근거로 쓴 자료' 목록이 이미 제공한다** — 답변 안의 `[ID]` 는 그 위의 편의다.
>
> 결론: **1.5B 로 본문과 인용을 동시에 얻는 방법을 아직 못 찾았다.** 더 큰 모델의 몫으로 남긴다.

#### I. `noHalluc` 을 긍정형으로 — 채택

`[가정]` 이었던 이중부정 가설이 확인됐다.

| 필드 문구 | 결과 |
|---|---|
| `noHalluc: 근거에 없는 사실을 지어내지 **않았는가**` | **4/4 전부 false** (잘 쓴 답변에도) |
| `allFromSource: 답변에 나온 사실이 **모두 자료 안에 있는가**` | **true 가 나오기 시작** |

> 1.5B 는 이중부정에서 방향을 놓친다. 판정 필드는 **긍정형으로 묻는다.**
> 같은 교훈의 세 번째 사례다 — 모델은 프롬프트에 쓰인 **문장 그대로**에 반응한다.

---

### 아직 안 돌린 회차

| 대상 | 무엇을 볼 것인가 |
|---|---|
| G 의 맞교환 원인 | 게이트가 받는 청크 구성이 바뀐 탓인지 `[가정]` 을 검증한다 |
| `GATE_K` 3 vs 4 vs 5 | 게이트 정확도의 최적점이 어디인가 |
| 더 큰 모델의 인용 | H 가 3b 이상에서는 되는가 (단, 3b 는 속도로 기각됨) |

> 🔴 **주의**: A 표의 기대 청크 15개는 **코퍼스를 만든 뒤에 붙인 주석**이다.
> 이 표를 보고 가중치를 맞추면 측정이 아니라 조정이 된다.
> 순위 방식을 바꾸면 **봉인 세트로 거절 성능도 함께 다시 잰다.**

---

## 13. 배포 및 수용 기준

**배포**: `vite build` → GitHub Pages. 배포 후 상대 경로 재확인.

### 수용 기준 (v3 개정 — 실측 반영)

> **v2 의 "10초 내 응답 시작" 은 폐기했다.** 근거 없이 정한 숫자였고 실측은 **약 25초**다.
> 8GB RAM · CPU 추론에서 지킬 수 없는 기준을 걸어 두면, 못 지킨 채로 남거나
> 지키려고 **거절 정확도를 팔게 된다.** 기준을 실측에 맞추되 무엇을 포기했는지 적는다.

#### 기능 — 통과해야 하는 것

- [x] Ollama 미실행 시 명확한 안내 + **다시 확인** 버튼
- [x] 봉인 자료 안 질문 → 근거 목록과 함께 답변
- [x] 봉인 자료 밖 질문 5개 → **전부 거절** (1층)
- [x] **범위 안처럼 보이나 자료에 답이 없는 질문 → 거절** (2층) — 진단 5/5
- [x] **`"53번은 몇 점인가요?"` 류 숫자 질문이 거절되지 않는다**
- [x] 공식/비공식 칩 구분 · 검색 경로(`vector`/`bm25`) 표시
- [x] 스트리밍 중 **중단** 정상 동작 · 중단해도 받은 답변 유지
- [x] 판정 배지 — **코드로 센 값과 모델 판정을 시각적으로 구분**
- [ ] **PBT/IBT 를 밝히지 않은 질문에 어느 기준인지 함께 답한다** — ❌ 모델이 규칙 3을 무시
- [ ] 답변 안에 `[ID]` 인용 — ❌ 0/4. §12 회차 H 참고. **1.5B 로는 본문과 맞바꿔야 한다**
- [ ] GitHub Pages URL 에서 **Chrome + `OLLAMA_ORIGINS` 설정 후** 정상 동작 — 미배포

#### 속도 — 단계별 예산으로 바꾼다

하나의 숫자로 재지 않는다. 어느 단계가 느린지 보이지 않으면 고칠 수 없다.

| 단계 | 콜드 | 웜 | 기준 | 근거 |
|---|---|---|---|---|
| 임베딩 모델 준비 | **39초** | 9~33초 | 콜드 60초 · 웜 40초 | 모델 약 120MB 다운로드 포함 |
| 질문 임베딩 + 검색 | < 1초 | < 1초 | 2초 | 벡터 65개는 무시할 부하 |
| **2층 게이트** | **~14초** | **~0.4초** | 콜드 20초 | 청크 4개 ≈ 560토큰 ÷ 35 tok/s |
| **첫 토큰까지** | **~12초** | — | 20초 | 청크 8개를 읽는 시간 |
| 답변 생성 | 22~74초 | — | — | **4.5 tok/s** · 답변 길이에 비례 |
| 판정 | 별도 왕복 | | | 답변 뒤에만 돈다 |

**질문 하나당 체감**: 콜드 **약 50초**, 웜 약 35초. 첫 방문은 여기에 **모델 준비 39초**가 더해진다.

> ⚠️ **웜 수치는 Ollama 의 KV 캐시 덕이다.** 같은 접두사의 프롬프트를 다시 보내면
> 프롬프트 처리를 건너뛴다(게이트 14초 → 0.4초). **비슷한 질문을 이어서 물을 때만 해당한다.**
> 측정할 때 이 효과를 통제하지 않으면 개선을 착각한다 — 실제로 두 번 착각했다.

#### 이 속도로 갈 수 있는 이유와, 포기한 것

- **속도를 위해 거절 정확도를 팔지 않는다.** 게이트를 빼면 즉시 14초가 빠지지만
  *"범위 안처럼 보이나 자료에 없는 질문"* 5/5 거절이 무너진다. §1 원칙이 §13 속도보다 우선한다
- **더 큰 모델은 기각됐다.** `3b` 는 질문당 165초 + 스왑 2.6GB (§12 회차 D)
- **남은 레버는 청크 수다.** 게이트 시간 ≈ 읽을 토큰 ÷ 35 이므로 `GATE_K` 가 직접 지배한다
- **스트리밍이 체감을 지탱한다.** 첫 토큰 12초 뒤부터 글자가 자라므로,
  전체 50초를 빈 화면으로 기다리지 않는다. **중단 버튼이 그 대기를 사용자 것으로 만든다**

#### 측정 재현

```bash
node scripts/bench-model.mjs qwen2.5:1.5b-instruct-q4_K_M   # 게이트·첫 토큰·tok/s
node scripts/bench-gate2.mjs                                # 게이트 지연 분해
node scripts/eval-gate.mjs                                  # 거절 정확도 25문항
node scripts/diag-rank.mjs                                  # 검색 recall·순위
```

> 측정 스크립트는 앱과 **같은 상수**(`GATE_K`·`ANSWER_K`)를 쓴다.
> 한때 앱은 8개, 스크립트는 5개를 넣어 **측정과 동작이 달랐다.** 상수를 공유해 막았다.
